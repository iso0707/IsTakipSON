name: APK Derle

# Ne zaman çalışsın:
#  - main/master'a her push'ta otomatik
#  - GitHub sayfasından "Run workflow" ile elle de tetiklenebilir
on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch: {}

jobs:
  build:
    # ubuntu-22.04 (ve daha yenisi) buildozer-action ile bazen
    # OpenJDK paket deposu (PPA) hatası veriyor ("does not have a
    # Release file"). ubuntu-20.04 bu action için bilinen en
    # kararlı sürüm, o yüzden onu kullanıyoruz.
    runs-on: ubuntu-20.04
    steps:
      - name: Kodu çek
        uses: actions/checkout@v4

      - name: Buildozer ile APK derle
        uses: ArtemSBulgakov/buildozer-action@v1
        id: buildozer
        with:
          # Her çalıştırmada temiz bir Ubuntu makinesi kullanılır,
          # bu yüzden eski/önbellek sorunu (fpdf2'nin pakete
          # girmemesi gibi) burada yaşanmaz.
          command: buildozer android debug
          buildozer_version: stable

      - name: APK'yı indirilebilir yap
        uses: actions/upload-artifact@v4
        with:
          name: istakip-apk
          path: ${{ steps.buildozer.outputs.filename }}
